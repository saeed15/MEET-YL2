from django.db import models
class polls(models=model):
	question-models.CharFeild(max_lenght = 200)
class awnser(models.mode)
	poll = models.Froeignkey(poll)
	awnser = models.CharFeied(max_lenght = 200)
	votes = models.IntegerField(default=0)	

# Crhttps://github.com/saeed15/MEET-YL2
